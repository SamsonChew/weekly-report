# hfcrypto 实盘复盘与排查（2026-06-14 / 15）

>  把 commit 的实盘代码跑起来；怀疑 dry-run **冷启动** + 表现不符合预期。
> 对 `/share/stats_200200003.csv`（06-12 06:27 → 06-15 02:10，**243,790 行 ≈ 2.82 天**）的实盘数据进行排查 & 数据分析
---

## ★ 最终结论速览（关键数据分析）

| # | 关键发现 | 关键数据 |
|---|---|---|
| 1 | **标的 = SOL** | `200200003 = SOL on deepcoin`（200=deepcoin）；$67 = SOL 6 月现价（2 月回测时 $124）|
| 2 | **模型 = 我们的 SOL MLP120** | deepcoin alpha 被 centers `[-4.64e-4, +4.72e-4]` 精确约束：**越界 0.0000%**、贴界 max=91%/min=98%；部署仓**只此一个模型** |
| 3 | **★ IC 衰减（核心）** | live 6 月 **0.2377** vs 回测 **0.3124** → 掉 **0.075（相对 −23%）** |
| 4 | **IC 时间序列(SOL)** | OOS 2 月 **0.3133** / 3 月 **0.3196** / 4 月 **0.3020**(稳)→ 6 月 live **0.2377**(掉)→ **根因=模型陈旧(2月训、~4月没重训)** |
| 5 | **延迟无损** | trade-mid(deepcoin) **0.2397** ≈ md-mid(binance) **0.2377** → 跨所延迟**不掉 IC 反而有显著提升，参考下面** |
| 6 | **逐日 IC 不升反降** | Day1 **0.278**(最高)→ 0.216 → 0.217 → 0.266 → 非 warm-up，"冷启动暖机变好"**数据不支持** |
| 7 | **alpha 尺度** | std **3.66e-5**、q99.9 **2.13e-4**、max **4.55e-4**(均 return 尺度,与 MLP120 一致) |

**一句话**:**同币、同模型、同特征源(binance),唯一变量是时间 → IC 从 0.31 衰减到 0.24 是模型陈旧,解法=用近期数据重训**;延迟不掉 IC,冷启动判不死(金标准 `corr vs Python` 冷 0.93/热 0.97)。

---

## 一、实盘数据复盘（stats_200200003，2.82 天）

> 方法：Python 离线复刻 `do_once` 发单（MLP return 参数 `alpha_lean=2.87 / pos_lean=0.002 / fee 5e-05 / gap 50ms`，持仓现金流）。
> （为什么不跑真 C++ run_sim：stats 是模型**输出**，不是 raw tick **输入**；run_sim 需 raw tick + 模型 + cfg 重算因子，这套 deepcoin 标的的原料我没有，只能拿已算好的 alpha 重放触发。）

### 任务 1：gross@mid / gross@fill vs 我们最好回测
| 配置 | gross@mid | gross@fill | net | 成交 | 来源 |
|------|-----------|------------|-----|------|------|
| SOL MLP 信号层 top0.5%(理想上界) | **+2.102** | — | +1.101 | — | §4.3 Python gate |
| SOL MLP C++ 热态 tt=3.5(2/18) | +1.470 | — | −0.139 | 1.45% | §4.5 run_sim |
| SOL MLP C++ 热态 tt=7(24天均) | **+1.77** | — | +0.18 | 166/天 | §4.5 run_sim |
| **deepcoin live tt=3.5** | +0.266 | −0.489 | −0.989 | 1700 | 本次 |
| **deepcoin live tt=5** | +0.490 | −0.287 | −0.787 | 384 | 本次 |
| **deepcoin live tt=7** | **+1.475** | **+0.540** | **+0.040** | 76 | 本次 |

- **tt=7：deepcoin gross@mid +1.48 ≈ SOL +1.77（同量级）**，gross@fill 转正 +0.54，**net +0.040 ≈ 0**（和 SOL +0.18 同档）。
- **共同点：都 ≈ 0**，穿价吃光 edge → **taker 共同死结**。tt=7 时 `net = gross@mid +1.48 − 穿价 0.94 − fee 0.5`；穿价随收紧变大（强信号时刻价差宽）。

### 任务 2：冷启动取证
| 证据 | 观察 | 指向 |
|------|------|------|
| 开头 30min alpha ramp | std 06:27 **1.05e-5** → 06:57 **3.14e-5** → 稳 ~4e-5（放大 ~3×） | ⚠️ 疑似冷（若已暖 bar1 就该满幅）；**但早晨低波动是 confound** |
| 总时长 2.82 天 < 8 天 | 多日因子（Sr12TradeImb）无论如何没暖透 | ⚠️ 除非启动回放 8 天，否则慢因子全程冷 |
| diff（基差）头 100 值 | 全 = 1.00000 | 基差本就 ~0，看不出暖机 |

→ 两条中等嫌疑指向"06-12 06:27 冷起"，但**都不是铁证**。

### 任务 3：IC/edge 随时间越来越好吗？——❌ 没有，Day1 最高
| 日期 | 行数 | IC_1s(Spearman) | top0.5% edge(1s) | alpha_std |
|------|------|-------|--------------|-----------|
| **06-12** | 63,172 | **+0.2784** ← 最高 | +0.647 | 4.13e-5 |
| 06-13 | 86,400 | **+0.2162** ← 最低 | +0.711 | 3.33e-5 |
| 06-14 | 86,400 | +0.2173 | +0.514 | 3.52e-5 |
| 06-15(半天) | 7,818 | +0.2782 | +0.878 | 4.43e-5 |

→ **IC 不升反降（Day1 最高），是 regime 波动不是 warm-up** → "冷启动暖机让指标越来越好"假设**数据不支持**。（注：IC 对冷热不敏感，所以"IC 不升"也不能反证它是热的。）

---

## 二、IC 对比与排查 → 定论：模型陈旧

### 主表：md-mid(binance) vs trade-mid(deepcoin) · 每天 1s IC（Pearson + Spearman）
| 日期 | 行数 | md-mid Pearson | md-mid Spearman | trade-mid Pearson | trade-mid Spearman |
|------|------|---|---|---|---|
| 06-12(半天) | 63,172 | +0.1957 | +0.2714 | +0.2228 | +0.2784 |
| 06-13(整天) | 86,400 | +0.1433 | +0.2155 | +0.1725 | +0.2162 |
| 06-14(整天) | 86,400 | +0.1547 | +0.2195 | +0.1785 | +0.2173 |
| 06-15(~2h) | 7,818 | +0.2310 | +0.2798 | +0.2455 | +0.2782 |
| **全程** | 243,790 | **+0.1699** | **+0.2377** | **+0.1955** | **+0.2397** |
| **SOL MLP120 回测对照(binance)** | 5.88M | **+0.2954** | **+0.3124** | — | — |

> 口径：mid = `(bp1+ap1)/2`；价基用 mid（信号质量），穿价的影响在 PnL（gross@fill）不在 IC。SOL 当年用 binance 训练/评估 → 对齐 **md-mid 列**（SOL 没在 bybit/deepcoin 算 IC）。基差≈0，deepcoin md/trade 几乎一样。
> **Spearman > Pearson**(全程 0.238 vs 0.170)→ 排序预测力强于幅度,典型短周期 alpha。

### 两个结论
1. **✅ deepcoin(执行所) vs binance(信号源) IC 几乎无差** —— trade-mid 0.2397 ≈ md-mid 0.2377。**跨所路由 deepcoin、延迟，没有明显 IC 损失。**
2. **⚠️ live 整体 IC vs 本地回测低 ~0.075** —— 0.2377 vs 0.3124 → **根因见下：模型陈旧。**

### 第二结论排查 → 模型陈旧（时间漂移）

**① 验证 `weights.json` = 我们的 SOL MLP120，就是它在产 deepcoin alpha（铁证）**
| 验证项 | 值 | 判定 |
|---|---|---|
| weights centers(return) | `[-4.641e-4 … +4.719e-4]`、120 特征 | = §4 那份 SOL MLP120 |
| deepcoin alpha 范围 | [−4.554e-4, +4.292e-4] | 完全落在 center [min,max] 内 |
| 越界占比 | **0.0000%** | center-decode 铁律满足 |
| 贴界程度 | max=**91%**×上界、min=**98%**×下界 | 贴着两端 → 就是这份 centers 在算 |
| 部署仓模型数 | **只有 `mlp_strong_120` 一个** | 线上就是这份 SOL MLP120 在服务 SOL |

**② 训练区间 + SOL OOS 逐月稳定性（OOS 稳到 4 月）**
| 阶段 | 区间 | Spearman_1s |
|---|---|---|
| **训练** | 2026-01-01 → **02-14**(45 天) | — |
| OOS 2 月 | 02-18 起 | **0.3133** |
| OOS 3 月 | 整月 | **0.3196** |
| OOS 4 月 | 到 04-26 | **0.3020** |
| **live 6 月** | 06-12→15 | **0.2377** |

**③ 定论**
```
1-2月训练 → 2-4月OOS稳0.31(头2.5月不掉) → [4月底-6月没测] → 6月 live 0.24
```
- 同币(SOL)、同模型(这份 weights)、同特征源(binance) → **唯一变量是时间**。
- **2 月训的模型 6 月还在用，~4 个月没重训 → IC 从 0.31 衰减到 0.24（相对掉 23%）。**
- 排除：冷启动(冷热 IC 只差 0.004)、延迟(结论1)、换币(已证是 SOL)。

**④ 解法：用近期数据(到 ~6 月)重训 MLP120** → 大概率提回 ~0.31。**信号没坏，就是该换新。** 训练天数(45天)够，**陈旧度(4 个月没重训)才是真问题。**

---

## 三、冷启动：机制 / 如何确保 / 如何排查

### 机制（代码无 warmup 开关，全靠 st_time）
- `run_sim.cc`、策略、因子里**搜不到 warmup/preheat/skip 任何逻辑**。热/冷 **100% 由 `data-provider.<venue>.raw_dp.st_time` 决定** —— 因子状态从数据第一刻累积。
- `st_time = 目标交易日` → **冷**(当天从零，坏一整天)；`st_time = 目标日−8天` → **热**(因子收敛)。

### 如何确保热启动
- **dry-run / run_sim**：`st_time ≤ 目标日−8天`，**只分析热透后的天**。
- **实盘 live（没捷径）**：进程**必须先吃 ≥8 天历史**(回放或提前空跑)才信 alpha；**头 8 天 alpha/下单不算数**。

### 如何判断冷热（金标准）
| 方法 | 冷 | 热 | 可靠性 |
|---|---|---|---|
| **corr vs Python** | ~0.926 | ~0.973 | ★ 金标准(对冷热敏感) |
| cfg st_time | =分析日 | ≤分析日−8天 | 直接 |
| IC | — | — | ✗ 不敏感(冷热差 0.004) |

### 如何排查这次 dry-run / 实盘到底怎么启动的（按能定论排序）
1. **核对两件事（最快）**：① genaAC 跑的是 `val` 还是 `warm8`？（val=冷）② 实盘 HFTaking `st_time` 设几号？有没有提前 ≥8 天回放？
2. 看启动日志 / 进程时长(≥8 天？这次才 2.8 天)。
3. 看实盘 cfg 的 `st_time`。
4. **corr vs Python**(金标准，需模型+原始数据)。
5. stats 弱信号(开头 alpha 偏小、IC 分段；判不死)。

### ⚠️ README 坑：默认示例是冷的
`crypto-ts-alpha/README.md` 有 3 个 genaAC cfg：`val`(单日冷,"不代表服务态")/ `warm`(49天)/ **`warm8`(8天热,"实盘推荐,corr≈0.973")**。但 **① 运行示例用 `val` ② 自检基准是 `val` ③ 流程"先 val 后 warm8"易停在 val** → 照抄易跑成冷。**且这是 genaAC(alpha 验证)，不是实盘 HFTaking 下单的预热。**

---

## 四、我后续的动作
1. **重训模型**（最优先）：用近期(到 ~6 月)数据重训 MLP120，把 IC 从 0.24 提回 ~0.31。**模型 4 个月没更新是 IC 下降的真因。**
2. **确保热启动**：实盘启动设 `st_time ≤ 上线日−8天`，或提前 8 天空跑；头 8 天不算数。README 补一条实盘(非 genaAC)预热指引，运行示例从 `val` 改 `warm8`。
3. **核对启动方式 + 定冷热**：核对这次到底是冷是热(见 §三);需要时拿模型+原始数据算 `corr vs Python` 给冷/热铁证。
4. **策略层试 maker**：taker 在成本墙下赚不到钱（tt=7 也 ≈0）→ 验证 **maker**（tt=7 的 gross@fill +0.54 在免穿价+返佣下可落袋）。
5. **上实时 feature importance 监控**（见 §五）—— 这次"模型陈旧"本可提前预警；实时盯每特征滚动 IC 能提前几周看出"该重训"，而非等实盘亏了才发现。

---

## 五、非线性模型(MLP)实时 feature importance 监控（开发方向）

> 背景：线性模型能"实时算过去 1h 每特征 IC"做输出确认 feature importance；MLP 非线性，要换方法。本节给**方法 + 工程权衡**。

### 5.1 为什么线性简单、MLP 难
```
线性:  alpha = Σ wᵢ·xᵢ   → 贡献 = wᵢ·xᵢ(权重恒定)→ 过去1h 每特征 IC = corr(xᵢ,y) 直接滚动算
MLP :  alpha = f(x) 非线性 → 没有恒定权重,得换方法
```

### 5.2 推荐方法：梯度归因（局部等效权重）
- MLP 在每个输入点**局部 = 线性**,等效权重 = 梯度:`wᵢ_eff(t) = ∂alpha/∂xᵢ`(随输入变 = MLP 的"实时权重")。
- 贡献:`(∂alpha/∂xᵢ)·xᵢ` —— **完全对标线性的 wᵢ·xᵢ**。
- **一次 backward 拿全部 120 个梯度**(120 入 1 出 → backward 比 forward 快),网络小 → **微秒级**。center-decode 梯度照样穿过 softmax 回到输入。

**滚动 1h 输出(对标线性那套):**
| 指标 | 公式 | 含义 |
|---|---|---|
| 重要性 | `mean(\|∂alpha/∂xᵢ·xᵢ\|)` | 这特征对 alpha 的平均驱动力(= 线性 \|wᵢxᵢ\|) |
| 预测力(model-free) | `corr(xᵢ, y_1h)` | 特征本身 IC(和线性同款,不依赖模型) |
| 贡献方向 | `corr(∂alpha/∂xᵢ·xᵢ, y_1h)` | MLP 给这特征的贡献方向对不对 |

**三个关键差别(vs 线性):**
1. **等效权重是时变的** —— 同特征在不同 regime 权重不同(MLP 自适应重加权);监控的是"当前权重"。
2. **一阶近似抓不全交互** —— 要更准用 **Integrated Gradients**(从基线积分,满足贡献加总守恒),成本 ~10-20 次前向 → **降频算**。
3. **裸 IC ≠ MLP 用没用它** —— `corr(xᵢ,y)` 高不代表 MLP 在用它(可能被别的特征替代);要"MLP 实际依赖哪些"必须看梯度归因。

### 5.3 ★ 工程权衡：别放交易关键路径,就几乎不加延迟
**核心:监控 ≠ 交易,两条路径分开:**
| 路径 | 干什么 | 延迟敏感? |
|---|---|---|
| **交易关键路径** | 因子 → 前向 → alpha → 触发 → 下单(200ms 都在这) | ★ 极敏感 |
| **监控路径** | 梯度 + 滚动 IC/贡献 | ✗ **不敏感,可异步、可降频** |

→ **放监控路径,交易下单一点不变。** 担心的延迟只在"硬塞进关键路径"时才发生。

**开销很小(量化):**
| 开销 | 大小 |
|---|---|
| 梯度计算 | ~0.1-0.2ms/bar(对照特征 200ms 可忽略) |
| 存储 | 滚动 1h:120×3600 ≈ 几 MB |
| 滚动 IC | 增量更新 O(120)/bar(用 online correlation,不重算) |

→ **真正的 trade-off 不是交易延迟,是"监控保真度/新鲜度 vs 工程复杂度"**:① 异步 vs 关键路径 ② 每 bar vs 每分钟(feature importance 不秒级突变,每分钟够) ③ gradient×input(便宜实时) vs Integrated Gradients(保真降频)。

**推荐架构:**
```
交易线程: 因子 → 前向 → alpha → 下单            (精简,不动)
            │ (复制 x、alpha 到队列)
监控线程: backward → gradient×input → 滚动1h IC/贡献 → 输出
            (异步、降频、单独进程也行;挂了不影响交易)
```

### 5.4 价值
**成本 = 几 MB + 一个异步线程;回报 = 模型退化早期预警**(本可提前几周抓到这次的 IC 0.31→0.24 衰减)。对交易延迟零影响,对风控价值大。
